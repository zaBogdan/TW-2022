exports.executeChain = async (message) => {
    // const chain = [

    // ];

    console.log('To execute chain', message)

    // try {
    //     for(const cmd of chain) {
    //         await cmd.execute();
    //     }
    // } catch(e) {
    //     console.error(e);
    // }
}